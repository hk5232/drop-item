<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>My Page</title>
        <!-- Favicon-->
        <link rel="icon" type="image/x-icon" href="assets/favicon.ico" />
        <!-- Bootstrap icons-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
        <!-- Core theme CSS (includes Bootstrap)-->
        <link href="css/styles.css" rel="stylesheet" />
    </head>
    <body>
        <%= render "shared/header" %>
        <!-- Header-->
        <header class="bg-secondary py-5">
            <div class="container px-4 px-lg-5 my-5">
                <div class="text-center text-white">
                    <h1 class="display-4 fw-bolder">My Page</h1>
                    <p class="lead fw-normal text-white-50 mb-0"><%= @user.nickname %>さんのページ</p>
                </div>
            </div>
        </header>
        <!-- Section-->
                <section class="py-5">
            <div class="container px-4 px-lg-5 mt-5 text-center">
               <h2 class="page-section-heading text-center text-uppercase text-secondary mb-0 py-2">Drop Items</h2>
                              <div class="divider-custom">
                    <div class="divider-custom-line"></div>
                    <div class="divider-custom-icon"><i class="fas fa-star"></i></div>
                    <div class="divider-custom-line"></div>
                </div>
                <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center text-center "> 
        <% if @items.blank? %>
      <div class="col mb-5">
        <%= link_to '#' do %>
        <div class="card h-100">
        <%= image_tag "https://tech-master.s3.amazonaws.com/uploads/curriculums/images/Rails1-4/sample.jpg", class: "item-img" %>
               <div class="text-center">
               <!-- Product name-->
              <h5 class="fw-bolder">落とし物情報サンプル</h5>
          
            <span>落とし物名称</span>
              <span class='star-count'>見つけた場所</span>
            </div>
          </div>

        <% end %>


      <% end %>
        <% @items.each do |item| %>
        <% if item.find.blank? %>
      <div class="col mb-5">
      <%= link_to item_path(item.id) do %>
        <div class='item-img-content card h-100'>
          <%= image_tag item.image, class: "item-img" %>

          <% if item.find.present? %>
          <div class='sold-out'>
            <span>発見済み</span>
          </div>
          <% end %>
        
        
       <div class="text-center">
          <h5 class='fw-bolder'>
            <%= item.name %></h5>
           <h5 class='fw-boldere'>
            <%= item.address %>
                                        <div class="card-footer p-4 pt-0 border-top-0 bg-transparent mt-2">
          <%= link_to "詳細を見に行く", item_path(item.id), class: "btn btn-outline-dark mt-auto" %>

                            </div>
          </div>
        </div>
         <% end %>
    </div>
      <% end %>      
      <% end %>
       </div>
<h2 class="page-section-heading text-center text-uppercase text-secondary mb-0 py-2">Found Items</h2>
                              <div class="divider-custom">
                    <div class="divider-custom-line"></div>
                    <div class="divider-custom-icon"><i class="fas fa-star"></i></div>
                    <div class="divider-custom-line"></div>
                </div>
                <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center text-center "> 
        <% @items.each do |item| %>
        <% if item.find.present? %>
      <div class="col mb-5">
      <%= link_to item_path(item.id) do %>
        <div class='item-img-content card h-100'>
          <%= image_tag item.image, class: "item-img" %>

          <% if item.find.present? %>
          <div class='sold-out'>
            <span>発見済み</span>
          </div>
          <% end %>
        
        
       <div class="text-center item-info">
          <h5 class='fw-bolder'>
            <%= item.name %></h5>
           <h5 class='fw-boldere'>
            <%= item.address %>
                                        <div class="card-footer p-4 pt-0 border-top-0 bg-transparent mt-2">
        <%= link_to "コメントを見に行く", item_path(item.id), class: "btn btn-outline-dark mt-auto" %>

                            </div>

          </div>
        </div>
         <% end %>
    </div>
      <% end %>      
      <% end %>
      
        </div>
                </div>
<footer class="footer text-center" style='width:100% !important; height:200px !important; margin-top :20px;' >
            <div class="container">
                <div class="row">
                    
                <div class="col-lg-12 mb-5 mb-lg-0">
                   <h1 class="text-uppercase mb-4">Drop Item&emsp;
                 
                      <%= link_to image_tag("app-store.svg", class:"apple-btn"), "#" %>
                       <%= link_to image_tag("google-play.png", class:"google-btn"), "#" %>
                   </h1>
                </div>
                    
                </div>
            </div>
        </footer>
        <!-- Bootstrap core JS-->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
        <!-- Core theme JS-->
        <script src="js/scripts.js"></script>
    </body>
</html>